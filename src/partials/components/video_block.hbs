{{!
	Secondary Story Block
	Params:
		image:	(string) image name with the extension
        image-alt: (string) Alternative text for image
        link: (string) URL to the main article
        category: (string) Category of the article
        title: (string) Title of the Article
		resume: (string) Short version of the article.
}}
<table class="story-wrapper inverse" cellpadding="0" cellspacing="0">
	<tr>
		<td>

            <table class="video-wraper">
                <tr>
                    <td>
                        <a class="secondary-story-img-link" href="{{ link }}" target="_blank">
                            <img src="../img/{{ image }}" alt="{{ image-alt }}">
                        </a>
                    </td>
                </tr>
            </table>

            {{#if category}}
                <p class="article-category">{{ category }}</p>
            {{/if}}
            <a href="{{ link }}" target="_blank">
                <h2 class="video-title">{{ title }}</h2>
            </a>
            <p class="video-resume">{{ resume }}</p>
		</td>
	</tr>
</table>